---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: d676437dfc39e1c241e09a446ca38c26_v3
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: d676437dfc39e1c241e09a446ca38c26_v3
---

# 考研题库 — SM-2 间隔重复复习系统实现计划 v3（定稿）

> 基础项目：`D:\tj\822\考研题库\index.html`（约 5800 行 Vanilla JS，单文件）
> 日期：2026-08-11
> v2 → v3：确认筛选栏保持 3 级合并、错题本扩面、毕业改掌握、复习模式全做

---

## 〇、现状分析与差异要点

### CSS 层已完成

5 级色板齐全：

| 级别 | status 值 | CSS 变量 | 颜色 | 含义 |
|------|-----------|----------|------|------|
| Lv5 | `proficient` | `--lv5` `#34C759` | 🟢 翠绿 | 熟练（完美） |
| Lv4 | `familiar` | `--lv4` `#00BFA5` | 🟩 青绿 | 较熟（正确但犹豫） |
| Lv3 | `vague` | `--lv3` `#FF9500` | 🟠 橙 | 模糊（思路对但卡壳） |
| Lv2 | `rusty` | `--lv2` `#FF6B35` | 🟧 深橙 | 困难（大部分忘记） |
| Lv1 | `wrong` | `--lv1` `#FF3B30` | 🔴 红 | 需复习/全忘 |

`.gel-btn.btn-status`、`.qnav button`、`.filter-btn` 的 5 级 CSS **已齐全**。

### 待办（JS + HTML）

| 模块 | 当前 | 目标 |
|------|------|------|
| mastery-bar 按钮 | 3 个 (Z/X/C) | 5 个 (Z / Z+X / X / X+C / C) |
| 键盘快捷键 | 3 键单键 | Z+X、X+C 组合键（200ms 窗口） |
| 筛选工具栏 | 4 项 | **保持 4 项**（熟练/模糊/需复习 各合并） + 复习 2 项 |
| 状态统计 bar | 4 类 | 6 类（5 级 + 未做） |
| 全局进度 | 3 级 | 5 级（环形图渐变） |
| 错题本 | 仅 lv1 | **lv1+lv2+lv3+lv4**（除 5 级外全收） |
| SM-2 复习 | 不存在 | 新建 |

---

## 一、核心原则

### 1.1 两层结构：标记精细 5 级，筛选粗放 3 级

| 层 | 粒度 | 用途 |
|----|------|------|
| **打标**（mastery-bar 按钮） | 5 级 | 精细记录掌握程度，驱动 SM-2 排期 |
| **筛选**（左侧 filter-toolbar） | 3 级（合并） | 快速过滤，保持简洁 |

合并规则：

| 筛选按钮 | 快捷键 | 涵盖 status 值 |
|----------|--------|---------------|
| 熟练 | Shift+Z | `proficient`(lv5) + `familiar`(lv4) |
| 模糊 | Shift+X | `vague`(lv3) + `rusty`(lv2) |
| 需复习 | Shift+C | `wrong`(lv1) |

### 1.2 复习与正常刷题统一

不再区分"复习模式 UI"和"正常刷题 UI"。通过 `reviewSession` 标记自动区分：
- 非复习会话 → 行为不变（首次打标跳题，改标不跳）
- 复习会话中 → 评分后必定跳队列下一题，同时更新 SM-2

### 1.3 参考方案

| 参考 | 借鉴点 |
|------|--------|
| **Anki** | 底部评分栏、到期倒计时、进度统计 |
| **SuperMemo** | SM-2 算法本身 |
| **RemNote** | 内联复习（不切独立 UI） |
| **Quizlet** | 掌握度渐变色视觉语言 |

### 1.4 UI 风格

延续现有**玻璃拟态 + 凝胶胶囊按钮 + 低饱和紫 `#660874`**主色调，所有新增 UI 复用现有 CSS 变量。

---

## 二、快捷键方案

### 2.1 打标快捷键（5 级 → 3 组合）

```
Z      → Lv5 熟练     (proficient)
Z + X  → Lv4 较熟练   (familiar)    // 先按 Z，200ms 内再按 X
X      → Lv3 模糊     (vague)
X + C  → Lv2 困难     (rusty)       // 先按 X，200ms 内再按 C
C      → Lv1 需复习   (wrong)
```

### 2.2 筛选快捷键（保持 3 级 + 复习）

```
Shift+Z → 熟练筛选（= lv5 + lv4）
Shift+X → 模糊筛选（= lv3 + lv2）
Shift+C → 需复习筛选（= lv1）
Shift+N → 带笔记
Shift+M → 复习到期（新增）
Shift+O → 已逾期（新增）
```

### 2.3 组合键实现

```javascript
let comboState = { z: false, x: false, timer: null };

function handleStatusKey(key) {
  if (key === 'z') {
    comboState.z = true;
    comboState.timer = setTimeout(() => { setStatus('proficient'); resetCombo(); }, 200);
  }
  if (key === 'x') {
    if (comboState.z) { clearTimeout(comboState.timer); setStatus('familiar'); resetCombo(); return; }
    comboState.x = true;
    comboState.timer = setTimeout(() => { setStatus('vague'); resetCombo(); }, 200);
  }
  if (key === 'c') {
    if (comboState.x) { clearTimeout(comboState.timer); setStatus('rusty'); resetCombo(); return; }
    setStatus('wrong');  // Lv1 立即触发
  }
}
```

**设计要点**：
- Z 单独按 → 等 200ms 无 X 才触发 Lv5（几乎无感知）
- X 单独按 → 等 200ms 无 C 才触发 Lv3
- C → 立即触发 Lv1（除非 X 等待中）
- 复习会话中同样走这套逻辑

### 2.4 mastery-bar 按钮

```html
<!-- 5 按钮等宽排列 -->
<button id="btnProficient" class="gel-btn btn-status" data-status="proficient">
  <span class="func-name">熟练</span><span class="key">Z</span></button>
<button id="btnFamiliar" class="gel-btn btn-status" data-status="familiar">
  <span class="func-name">较熟练</span><span class="key">Z+X</span></button>
<button id="btnVague" class="gel-btn btn-status" data-status="vague">
  <span class="func-name">模糊</span><span class="key">X</span></button>
<button id="btnRusty" class="gel-btn btn-status" data-status="rusty">
  <span class="func-name">困难</span><span class="key">X+C</span></button>
<button id="btnWrong" class="gel-btn btn-status" data-status="wrong">
  <span class="func-name">需复习</span><span class="key">C</span></button>
```

---

## 三、筛选工具栏（合并 3 级）

### 3.1 HTML

```html
<div class="filter-toolbar" id="filterToolbar">
  <div class="filter-label">状态筛选</div>
  <div class="filter-hint">可组合选择</div>
  <button class="filter-btn active" data-filter="all">全部
    <span class="filter-count"></span><span class="filter-key">Shift+A</span></button>
  <button class="filter-btn" data-filter="proficient">熟练
    <span class="filter-count"></span><span class="filter-key">Shift+Z</span></button>
  <button class="filter-btn" data-filter="vague">模糊
    <span class="filter-count"></span><span class="filter-key">Shift+X</span></button>
  <button class="filter-btn" data-filter="wrong">需复习
    <span class="filter-count"></span><span class="filter-key">Shift+C</span></button>
  <button class="filter-btn" data-filter="unmarked">带笔记
    <span class="filter-count"></span><span class="filter-key">Shift+N</span></button>
  <!-- 新增：复习相关筛选 -->
  <button class="filter-btn" data-filter="sm2-due">到期
    <span class="filter-count"></span><span class="filter-key">Shift+M</span></button>
  <button class="filter-btn" data-filter="sm2-overdue">逾期
    <span class="filter-count"></span><span class="filter-key">Shift+O</span></button>
</div>
```

### 3.2 筛选逻辑

```javascript
function getFilteredIndices() {
  const ch = getChapter();
  const all = Array.from({ length: ch.total }, (_, i) => i);
  if (isAllFilterActive()) return all;
  return all.filter(i => {
    if (currentFilters.has('unmarked')) {
      if (notesData[notesKeyFor(i)] || hasQuestionImagesAnnotated(i)) return true;
    }
    const s = statuses[i] || '';

    // 合并筛选：熟练 = lv4+familiar + lv5+proficient
    if (currentFilters.has('proficient') && (s === 'proficient' || s === 'familiar'))
      return true;
    // 模糊 = lv3+vague + lv2+rusty
    if (currentFilters.has('vague') && (s === 'vague' || s === 'rusty'))
      return true;
    // 需复习 = lv1+wrong
    if (currentFilters.has('wrong') && s === 'wrong')
      return true;
    return false;
  });
}
```

### 3.3 筛选统计

```javascript
function updateFilterCounts() {
  const ch = getChapter();
  let prof = 0, vag = 0, wr = 0;
  for (let i = 0; i < ch.total; i++) {
    const s = statuses[i];
    if (s === 'proficient' || s === 'familiar') prof++;      // 熟练 = lv5+lv4
    else if (s === 'vague' || s === 'rusty') vag++;           // 模糊 = lv3+lv2
    else if (s === 'wrong') wr++;                              // 需复习 = lv1
  }
  // 更新 filter-btn 的 filter-count
}
```

---

## 四、SM-2 算法

### 4.1 参数

| 参数 | 含义 | 初始值 | 范围 |
|------|------|--------|------|
| EF | 难度因子 | 2.5 | ≥ 1.3 |
| Interval | 当前间隔（天） | 1 | ≥ 1 |
| Reps | 成功连续复习次数 | 0 | ≥ 0 |
| nextReview | 下次复习日期（ms 时间戳） | — | — |
| lastReview | 上次复习日期 | — | — |
| history | 复习历史 | [] | — |

### 4.2 评分映射

| 评分 | status | 快捷键 | EF 变化 | Reps | Interval |
|------|--------|--------|---------|------|----------|
| 5 | proficient | Z | +0.10 | ++ | × EF |
| 4 | familiar | Z+X | +0.00 | ++ | × EF |
| 3 | vague | X | −0.14 | 0 | 1 |
| 2 | rusty | X+C | −0.22 | 0 | 1 |
| 1 | wrong | C | −0.30 | 0 | 1 |

### 4.3 核心算法

```javascript
function calcSM2(record, score) {
  if (!record) record = { ef: 2.5, interval: 1, reps: 0, nextReview: 0, lastReview: 0, history: [] };
  const now = Date.now();
  let { ef, interval, reps } = record;
  const deltaMap = { 5: 0.10, 4: 0.00, 3: -0.14, 2: -0.22, 1: -0.30 };
  const delta = deltaMap[score] || 0;

  if (score >= 3) {
    if (reps === 0) interval = 1;
    else if (reps === 1) interval = 6;
    else interval = Math.round(interval * ef);
    reps++;
  } else {
    interval = 1;
    reps = 0;
  }

  ef = Math.max(1.3, ef + delta);
  const nextReview = now + interval * 86400000;

  record.history = record.history || [];
  record.history.push({ date: now, score, ef, interval });

  return { ...record, ef, interval, reps, nextReview, lastReview: now };
}

// 掌握条件：连续 3 次评分 ≥ 4 且 interval > 90 天
function checkMastered(record) {
  const h = record.history || [];
  if (h.length < 3) return false;
  return h.slice(-3).every(e => e.score >= 4) && record.interval > 90;
}
```

---

## 五、数据结构

### 5.1 存储键

```
localStorage key: sm2_{chapterId}_{storageSuffix}
```

与现有 `statuses` 按书分离一致。1000题合并章节跟随父书统一存储。

### 5.2 单题 SM-2 记录

```json
{
  "ef": 2.5,
  "interval": 1,
  "reps": 0,
  "nextReview": 1754841600000,
  "lastReview": 1754755200000,
  "history": [
    { "date": 1754582400000, "score": 3, "ef": 2.36, "interval": 1 }
  ]
}
```

### 5.3 内存态

```javascript
let sm2 = {};            // { idx: SM2Record }
let reviewQueue = [];    // [{ chapterId, index, record }]
let reviewSession = null; // { queue, currentIdx, mode: 'sequential'|'random'|'overdue' }
```

---

## 六、UI 设计

### 6.1 章节统计 bar（6 项）

```
熟练 18  ·  较熟 12  ·  模糊 5  ·  困难 3  ·  需复习 2  ·  未做 8
🟢        🟩         🟠        🟧         🔴           ⚪
```

进度条分段着色（5 色渐变）：

```css
.stats-bar-fill {
  background: linear-gradient(90deg,
    var(--lv5) 0%, var(--lv4) 40%, var(--lv3) 60%, var(--lv2) 80%, var(--lv1) 100%);
}
```

### 6.2 复习面板（M 键触发）

```
┌──────────────────────────────────────────────┐
│  📊 间隔重复复习                          [×] │
│                                              │
│  ┌─────────┬─────────┬─────────┬─────────┐  │
│  │ 今日到期 │  已逾期  │  队列中  │  已掌握  │  │
│  │   12    │    5    │   47    │   238   │  │
│  │  🔵     │  🔴     │  🟣     │  ⭐     │  │
│  └─────────┴─────────┴─────────┴─────────┘  │
│                                              │
│  刷题模式：○ 顺序  ○ 随机  ○ 仅逾期        │
│  [开始全部复习]                              │
│                                              │
│  ▼ 按章节展开                                │
│  ┌ 基础30讲 ──────────────────────────────┐ │
│  │ 高数第1讲    到期3  逾期1    [复习]    │ │
│  │ 高数第2讲    到期2  逾期0    [复习]    │ │
│  └────────────────────────────────────────┘ │
│  ┌ 1000题（合并到 30讲章节）    [全部复习]  │ │
│  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

### 6.3 复习会话状态指示器

标题栏叠加（无侵入）：

```
基础30讲 高数 高数第1讲  [📋复习中 3/12] [结束]
```

mastery-bar 下方新增 SM-2 信息行：

```
EF: 2.5  |  间隔: 6天  |  复习次数: 2  |  上次: 2026-08-04
```

### 6.4 qnav 视觉标记

| 状态 | 效果 | CSS |
|------|------|-----|
| 今日到期 | 🔵 蓝色呼吸边框 | `@keyframes sm2-blink` 2s |
| 已逾期（>1天） | 🔴 红色波纹脉冲 | `@keyframes sm2-ripple` 1.5s |
| 在队列中 | 🟣 右上角紫色小圆点 | `::after` 6px dot |
| 已掌握 | ⭐ 右下角金色小星 | `::after "★"` gold |
| 无记录 | 无变化 | — |

### 6.5 错题本

收集 lv1+lv2+lv3+lv4（除 5 级外全收）。错题本列表沿用现有 UI，分组标题从「需复习」改为按等级显示：

```
需复习 🔴  3题
困难 🟧    5题
模糊 🟠    8题
较熟练 🟩  12题
```

---

## 七、实施步骤（9 步，约 500 行增量）

### 步骤 1：组合键系统（~55 行）

新增 `handleStatusKey()` + `resetCombo()`，修改 `keydown` 中 Z/X/C 处理。

### 步骤 2：mastery-bar 5 按钮（~40 行）

HTML 加 2 按钮（btnFamiliar、btnRusty）+ `updateStatusBtns()` 支持 5 级。

### 步骤 3：筛选栏合并逻辑（~40 行）

`getFilteredIndices()` 改为合并匹配（熟练=l5+l4，模糊=l3+l2），`updateFilterCounts()` 合并计数。新增 Shift+M/Shift+O 筛选。

### 步骤 4：统计系统 5 级（~35 行）

`renderStats()` 6 项统计 + `getChStats()` 返回 5 个字段 + 进度条 5 色渐变。

### 步骤 5：SM-2 核心算法（~30 行）

`calcSM2()` + `checkMastered()` + `isDue()`。

### 步骤 6：SM-2 数据持久化（~35 行）

`loadSM2()` / `saveSM2()` + `sm2Review()`。修改 `setStatus()` 末尾同步 SM-2 评分 + 复习会话自动跳题。

### 步骤 7：复习面板 UI（~85 行）

`toggleSM2Panel()` / `renderSM2Panel()` / `buildReviewQueue()` / `renderChapterReviewRows()`。4 统计卡片 + 章节展开列表 + 模式选择。

### 步骤 8：复习会话管理（~55 行）

`startReview(chapterIds, mode)` / `reviewNext()` / `exitReviewSession()`。支持 `sequential`（顺序）、`random`（随机）、`overdue`（仅逾期）三个模式。

### 步骤 9：qnav 标记 + 全局进度 + 错题本（~70 行）

`renderSM2Dots()` + CSS 动画 + 全局进度环形图 5 色 + 错题本扩大收集范围。

---

## 八、快捷键总表

### 打标（正常 & 复习通用）

| 快捷键 | 等级 |
|--------|------|
| Z | Lv5 熟练 |
| Z→X (200ms) | Lv4 较熟练 |
| X | Lv3 模糊 |
| X→C (200ms) | Lv2 困难 |
| C | Lv1 需复习 |

### 筛选（保持 3 级合并）

| 快捷键 | 筛选 |
|--------|------|
| Shift+A | 全部 |
| Shift+Z | 熟练（lv5+lv4） |
| Shift+X | 模糊（lv3+lv2） |
| Shift+C | 需复习（lv1） |
| Shift+N | 带笔记 |
| Shift+M | 复习到期 |
| Shift+O | 已逾期 |

### 面板 & 导航

| 快捷键 | 功能 |
|--------|------|
| M | 间隔重复复习面板 |
| V | 全局进度 |
| B | 错题本 |
| G | 切换科目 |
| H | 快捷键帮助 |
| Esc | 关闭面板/退出复习 |

---

## 九、验收标准

- [ ] Z/X/C 单键 → Lv5/Lv3/Lv1，按钮和数据正确
- [ ] Z+X 组合 → Lv4，X+C 组合 → Lv2
- [ ] mastery-bar 5 按钮等宽，激活态颜色正确
- [ ] 筛选栏 4 按钮（全部/熟练/模糊/需复习）+ 带笔记 + 复习2项，合并筛选正确
- [ ] 统计栏 6 项，进度条 5 色渐变
- [ ] M 键打开/关闭复习面板，统计准确
- [ ] 复习面板按章节展开，章节级 [复习] 按钮 + 全局 [开始全部复习]
- [ ] 顺序/随机/仅逾期 三种模式均正常
- [ ] 复习会话中相同快捷键评分，自动跳题
- [ ] 复习会话进度 + SM-2 状态行正确
- [ ] SM-2 数据写入 localStorage（sm2_ 前缀）
- [ ] 到期/逾期/队列中/掌握 四种 qnav 标记正确
- [ ] 连续 3 次 ≥ 4 + interval > 90 → 掌握（⭐）
- [ ] 全局进度各章 5 色环形图
- [ ] 错题本收集 lv1~lv4，按等级分组
- [ ] 刷新后 SM-2 数据不丢失
- [ ] 切换科目后 SM-2 正确加载
- [ ] 存量 3 级数据兼容
- [ ] Esc 退出复习

---

## 十、文件变更

| 文件 | 说明 |
|------|------|
| `index.html` | HTML + CSS + JS，约 +500 行增量 |

---

*（内容由 AI 生成，仅供参考）*
